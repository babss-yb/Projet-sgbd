services:
  # Backend API
  - type: web
    name: transpobot-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DB_HOST
        fromDatabase:
          name: transpobot-db
          property: host
      - key: DB_USER
        fromDatabase:
          name: transpobot-db
          property: username
      - key: DB_PASSWORD
        fromDatabase:
          name: transpobot-db
          property: password
      - key: DB_NAME
        fromDatabase:
          name: transpobot-db
          property: database
      - key: DB_PORT
        fromDatabase:
          name: transpobot-db
          property: port
      - key: OPENAI_API_KEY
        generateValue: true

  # Frontend React
  - type: web
    name: transpobot-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build # Changé de publishDir à staticPublishPath
    envVars:
      - key: REACT_APP_API_URL
        value: https://transpobot-backend.onrender.com

databases:
  - name: transpobot-db
    databaseName: transpobot
    plan: free
